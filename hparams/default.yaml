# Model: wav2vec2 + DNN + CTC
# Augmentation: SpecAugment
# Authors: Titouan Parcollet 2021
#          Mirco Ravanelli 2021
#          Sangeet Sagar 2022
# ################################

# Seed needs to be set at top of yaml, before objects with parameters are made
seed: 8200
__set_seed: !apply:speechbrain.utils.seed_everything [!ref <seed>]
output_folder: !ref ./results/CTC_w2v_<precision>/
test_wer_file: !ref <output_folder>/wer_test.txt
save_folder: !ref <output_folder>/save
train_log: !ref <output_folder>/train_log.txt

# URL for the LARGE Fairseq German wav2vec2 model.
wav2vec2_hub: facebook/wav2vec2-large-xlsr-53-german
wav2vec2_folder: !ref <save_folder>/wav2vec2_checkpoint

# Dataset prep parameters
data_folder: !PLACEHOLDER
train_tsv_file: !ref <data_folder>/train.tsv
dev_tsv_file: !ref <data_folder>/dev.tsv
test_tsv_file: !ref <data_folder>/test.tsv
accented_letters: True
language: de
train_csv: !ref <save_folder>/train.csv
valid_csv: !ref <save_folder>/dev.csv
test_csv: !ref <save_folder>/test.csv
skip_prep: False

# We remove utterance slonger than 10s in the train/dev/test sets as
# longer sentences certainly correspond to "open microphones".
avoid_if_longer_than: 10.0

batch_size: 1
test_batch_size: 1
precision: fp32  # bf16, fp16 or fp32

win_length: 2048
hop_length: 512
n_fft_psy: 2048

eval_checkpoint_path: !PLACEHOLDER

######## Attack Parameters ##########

adv_type: hybrid_pure_gaussian # hybrid_pure hybrid_pure_gaussian hybrid_separate hybrid_separate_gaussian # dist # pvp
train_adv_flg: 1 # 0 1
eval_precision:  fp32 # fp16, bf16, fp32
adv_WER: !ref wer_<adv_type>_<precision>_<eval_precision>.txt
clean_audio_adv_transcripts: ./results/clean_audio_adv_transcripts.csv
cw_attack_eps: 1.0
cw_attack_max_iter: 1000
cw_attack_learning_rate: 0.002
cw_attack_global_max_length: 562480
cw_attack_initial_rescale: 1.0
cw_attack_decrease_factor_eps: 0.5
cw_attack_num_iter_decrease_eps: 1
cw_attack_clip_min: null
cw_attack_clip_max: null
cw_attack_const: 10
cw_attack_targeted: true
cw_attack_alpha: 0.3
cw_attack_optimizer: null
cw_attack_optim_arg: null
adv_audio_adv_transcripts: !ref <output_folder>/adv_audio_adv_transcripts.csv
clean_WER: !ref wer_100_<precision>_<eval_precision>.txt
clean_subset_WER: !ref wer_clean_subset_<precision>_<eval_precision>.txt
clean_audio_clean_transcripts: ./results/clean_audio_clean_transcripts.csv
path_adapt : !ref <output_folder>/adv_audio/<adv_type>/<precision>
succesfull_adv: !ref <output_folder>/succesfull<adv_type>_<precision>_<eval_precision>.csv
unsuccesfull_adv: !ref <output_folder>/unsuccesfull<adv_type>_<precision>_<eval_precision>.csv
output_wer_folder: !ref <output_folder>/wer
cw_audio_adv_transcripts: ./results/cw_audio_adv_transcripts.csv

########### Defensive Parameters ##########################

val_benign: ./results/clean_audio_clean_transcripts_eval.csv
val_adv: !ref <output_folder>/adv_audio_adv_transcripts_eval.csv
train_benign: ./results/clean_audio_clean_transcripts_fit.csv

nn_detector_path: !ref <distriblock_folder>/detector_separate
gaussian_detector_path: !ref <distriblock_folder>/gaussian_detector

distriblock_folder: distriblock_pure

####################### Training Parameters ####################################

number_of_epochs: 45
lr: 1.0
lr_wav2vec: 0.0001
sorting: ascending
sample_rate: 16000
ckpt_interval_minutes: 30 # save checkpoint every N min

# With data_parallel batch_size is split into N jobs
# With DDP batch_size is multiplied by N jobs
# Must be 6 per GPU to fit 16GB of VRAM
dynamic_batching: False
dataloader_num_workers: 8
test_num_workers: 8

dataloader_options:
    batch_size: !ref <batch_size>
    num_workers: !ref <dataloader_num_workers>
test_dataloader_options:
    batch_size: !ref <test_batch_size>
    num_workers: !ref <test_num_workers>

# BPE parameters
token_type: char # ["unigram", "bpe", "char"]
character_coverage: 1.0

####################### Model Parameters #######################################
# activation: !name:torch.nn.LeakyReLU
dnn_neurons: 1024
wav2vec_output_dim: !ref <dnn_neurons>
freeze_wav2vec: False
freeze_feature_extractor: False
dropout: 0.15
warmup_steps: 500

# Outputs
output_neurons: 32 # BPE size, index(blank/eos/bos) = 0

# Be sure that the bos and eos index match with the BPEs ones
blank_index: 0
bos_index: 1
eos_index: 2

# Decoding parameters
test_beam_search:
    blank_index: !ref <blank_index>
    beam_size: 100
    beam_prune_logp: -12.0
    token_prune_min_logp: -1.2
    prune_history: True
    topk: 1
    alpha: 1.0
    beta: 0.5
    # To use n-gram LM for decoding, follow steps in README.md.
    # kenlm_model_path: none

# Functions and classes
epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>

############################## Augmentations ###################################

# Speed perturbation
speed_perturb: !new:speechbrain.augment.time_domain.SpeedPerturb
    orig_freq: !ref <sample_rate>
    speeds: [95, 100, 105]

# Frequency drop: randomly drops a number of frequency bands to zero.
drop_freq: !new:speechbrain.augment.time_domain.DropFreq
    drop_freq_low: 0
    drop_freq_high: 1
    drop_freq_count_low: 1
    drop_freq_count_high: 3
    drop_freq_width: 0.05

# Time drop: randomly drops a number of temporal chunks.
drop_chunk: !new:speechbrain.augment.time_domain.DropChunk
    drop_length_low: 1000
    drop_length_high: 2000
    drop_count_low: 1
    drop_count_high: 5

# Augmenter: Combines previously defined augmentations to perform data augmentation
wav_augment: !new:speechbrain.augment.augmenter.Augmenter
    min_augmentations: 3
    max_augmentations: 3
    augment_prob: 1.0
    augmentations: [
        !ref <speed_perturb>,
        !ref <drop_freq>,
        !ref <drop_chunk>]

############################## Models ##########################################

enc: !new:speechbrain.nnet.containers.Sequential
    input_shape: [null, null, !ref <wav2vec_output_dim>]
    linear1: !name:speechbrain.nnet.linear.Linear
        n_neurons: !ref <dnn_neurons>
        bias: True
    bn1: !name:speechbrain.nnet.normalization.BatchNorm1d
    activation: !new:torch.nn.LeakyReLU
    drop: !new:torch.nn.Dropout
        p: !ref <dropout>
    linear2: !name:speechbrain.nnet.linear.Linear
        n_neurons: !ref <dnn_neurons>
        bias: True
    bn2: !name:speechbrain.nnet.normalization.BatchNorm1d
    activation2: !new:torch.nn.LeakyReLU
    drop2: !new:torch.nn.Dropout
        p: !ref <dropout>
    linear3: !name:speechbrain.nnet.linear.Linear
        n_neurons: !ref <dnn_neurons>
        bias: True
    bn3: !name:speechbrain.nnet.normalization.BatchNorm1d
    activation3: !new:torch.nn.LeakyReLU

wav2vec2: !new:speechbrain.integrations.huggingface.wav2vec2.Wav2Vec2
    source: !ref <wav2vec2_hub>
    output_norm: True
    freeze: !ref <freeze_wav2vec>
    freeze_feature_extractor: !ref <freeze_feature_extractor>
    save_path: !ref <wav2vec2_folder>

#####
# Uncomment this block if you prefer to use a Fairseq pretrained model instead
# of a HuggingFace one. Here, we provide an URL that is obtained from the
# Fairseq github for the multilingual XLSR.
#
#wav2vec2_url: https://dl.fbaipublicfiles.com/fairseq/wav2vec/wav2vec_vox_960h_pl.pt
#wav2vec2: !new:speechbrain.lobes.models.fairseq_wav2vec.FairseqWav2Vec2
#    pretrained_path: !ref <wav2vec2_url>
#    output_norm: True
#    freeze: False
#    save_path: !ref <save_folder>/wav2vec2_checkpoint/model.pt

ctc_lin: !new:speechbrain.nnet.linear.Linear
    input_size: !ref <dnn_neurons>
    n_neurons: !ref <output_neurons>

log_softmax: !new:speechbrain.nnet.activations.Softmax
    apply_log: True

ctc_cost: !name:speechbrain.nnet.losses.ctc_loss
    blank_index: !ref <blank_index>

modules:
    wav2vec2: !ref <wav2vec2>
    enc: !ref <enc>
    ctc_lin: !ref <ctc_lin>

model: !new:torch.nn.ModuleList
    - [!ref <enc>, !ref <ctc_lin>]

model_opt_class: !name:torch.optim.Adadelta
    lr: !ref <lr>
    rho: 0.95
    eps: 1.e-8

wav2vec_opt_class: !name:torch.optim.Adam
    lr: !ref <lr_wav2vec>

lr_annealing_model: !new:speechbrain.nnet.schedulers.NewBobScheduler
    initial_value: !ref <lr>
    improvement_threshold: 0.0025
    annealing_factor: 0.8
    patient: 0

lr_annealing_wav2vec: !new:speechbrain.nnet.schedulers.NewBobScheduler
    initial_value: !ref <lr_wav2vec>
    improvement_threshold: 0.0025
    annealing_factor: 0.9
    patient: 0

checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <eval_checkpoint_path>
    recoverables:
        wav2vec2: !ref <wav2vec2>
        model: !ref <model>
        scheduler_model: !ref <lr_annealing_model>
        scheduler_wav2vec: !ref <lr_annealing_wav2vec>
        counter: !ref <epoch_counter>

train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <train_log>

error_rate_computer: !name:speechbrain.utils.metric_stats.ErrorRateStats

cer_computer: !name:speechbrain.utils.metric_stats.ErrorRateStats
    split_tokens: True
