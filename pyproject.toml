[project]
name = "precision-varying-distriblock"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "speechbrain>=1.1.0",
]
